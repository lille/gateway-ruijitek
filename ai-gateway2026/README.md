# ai-gateway2026

示例项目：AI 网关演示平台和工具集合。

包含：
- 演示前端 `web-demo` 和 `android-app`。
- 网关服务示例 `server.js` 与配置文件。
- 部署脚本与打包说明（见 `deploy/`）。

使用方法（示例）：
1. 在项目根目录运行 `node server.js` 启动演示服务。
2. 打开 `web-demo/index.html` 查看实时数据展示页面。

更多信息见 `docs/` 目录。